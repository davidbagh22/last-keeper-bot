# Production v4 validation

This branch exists only to run the complete pull-request validation suite against the current production code: compilation, unit tests, persistent-storage checks, route tests and the 50-game catalog.
